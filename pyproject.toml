[project]
name = "glioma-project"
version = "0.1.0"
description = "Parameter estimation for glioblastoma growth models"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"